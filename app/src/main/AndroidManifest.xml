<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.geekstorming.storymapper">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.login.LoginActivity"/>
        <activity android:name=".ui.books.BooksActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ui.characters.CharactersActivity">
        </activity>
        <activity android:name=".ui.characters.AddCharacterActivity"/>
        <activity android:name=".ui.characters.EditCharacterActivity"/>
        <activity android:name=".ui.about.AboutActivity"/>
        <activity android:name=".ui.settings.SettingsActivity"/>
        <activity android:name=".ui.books.AddBookActivity">
        </activity>
        <activity android:name=".ui.books.EditBookActivity">
        </activity>
    </application>

</manifest>